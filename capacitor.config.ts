import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moneycalculator.app',
  appName: 'SaveWise',
  webDir: 'dist/money-calculator/browser',
};

export default config;
